{
  "name": "susuchain-subgraph",
  "version": "1.0.0",
  "description": "SusuChain subgraph for Base network",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ susuchain-base",
    "create-local": "graph create --node http://localhost:8020/ susuchain-base",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 susuchain-base"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
